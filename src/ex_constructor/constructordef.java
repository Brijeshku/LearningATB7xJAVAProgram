package ex_constructor;

public class constructordef {

    // Constructor -Is a special methods , it will call automatically when we defines
   // new claasname()
    // Constructor always called by new keyword
    // types
    // Default const-70%- Hard coded value
    //Parameters const-25%- we can ask user to initialize the value
    // Match the value from calling
    //Copy const- Rare used 1-5% no one use max
    //This vs super ? why used
    //  static keywords and static methods

    // IMP- For constructor , it should be the same name
    // we can create with same name by using void


}
